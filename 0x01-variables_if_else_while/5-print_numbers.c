#include <stdio.h>

/**
 * main - print if the number is positive, zero, or negative
 *
 * Description: using main and printf functions
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%i", n);
	}
	printf('\n');
	return (0);
}
