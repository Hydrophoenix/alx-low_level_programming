#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10
 *
 * Description: use main and putchar functions
 * Return: 0
 */
int main(void)
{
	int n;

	for(n = 0; n <= 0; n++)
	{
		putchar("%i", n);
	}
	putchar('\n');
	return (0);
}
